Status: permission/tooling needed

Needed capability:
- <specific OAuth scope, platform role, MCP, CLI, plugin, profile toolset, environment, or fixture>

Why it is needed:
- <work outcome blocked by this capability>

Narrowest safe grant/change:
- <least-privilege access or minimal tool/config change>

Verification command/check after granted:
```bash
<read-only command or health check>
```

Security boundary:
- Do not paste secrets in chat or issue comments.
- Use platform auth, managed secret stores, or user-completed auth flows.

Fallback if not granted:
- <safe reduced-scope path>

Created by Victor
